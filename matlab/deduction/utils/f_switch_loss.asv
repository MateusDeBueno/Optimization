function [ZVS_p, ZVS_s] = f_switch_loss(I_sw_p_on,I_sw_s_on,fs)

    load('f_fitted_off.mat')
    load('f_fitted_on.mat')

    if (I_sw_p_on < 0)
        ZVS_p = 1;
    else
        ZVS_p = 0;
    end

    if (I_sw_s_on < 0)
        ZVS_s = 1;
    else
        ZVS_s = 0;
    end

    if (ZVS_p == 1)
        p_turn
    else

    end
    
end